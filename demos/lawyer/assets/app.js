document.addEventListener("DOMContentLoaded",()=>{const pre=document.querySelector(".preloader");setTimeout(()=>pre?.classList.add("done"),650);const nav=document.querySelector(".site-nav");window.addEventListener("scroll",()=>{if(nav)nav.classList.toggle("scrolled",scrollY>30)});const toggle=document.querySelector(".menu-toggle");const navLinks=document.querySelector(".site-nav nav");toggle?.addEventListener("click",()=>{if(!navLinks)return;navLinks.classList.toggle("mobile-open")});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll(".reveal").forEach(x=>io.observe(x));const service=new URLSearchParams(location.search).get("service");const select=document.querySelector('select[name="service"]');if(service&&select){[...select.options].forEach(o=>{if(o.text===service)o.selected=true})}});